<!--pages/food/map.wxml-->
<map class="restaurants-map" longitude="{{centerLongitude}}" latitude="{{centerLatitude}}" scale="{{scale}}" markers="{{markers}}" bindmarkertap="bindMarkerTap" subkey="{{key}}">
</map>